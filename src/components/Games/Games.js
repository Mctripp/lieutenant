import React from 'react'

const Games = () => {
  return (
    <React.Fragment>
      <p>This is the games page.<br></br><br></br>
      It is currently under construction, but when signed in, will display
      games a user has played in, as well as in-depth stats about
      those games when navigating to a specific one.
      </p>
    </React.Fragment>
  )
}

export default Games
